can you read the get_package_info() and the method that gets the spec_questions() from the featurization package, utilize the notebook_utils of the dd_parser_cleaner package to get the meta-data table and the cleaned dataset and then create the survival dataset in the attached notebook. The event to be tracked is ticket closure, use the incident_state for that. use the last ticket closure as the end of the observation period, go back 1 year to get the start of the observation period, include a duration attribute for closed tickets which is the time between opened_at and closed_at, and include only support groups with more than 20 closure events in the final dataset. Drop records where `assignment_group == '?'` before applying the threshold filter. Define this threshold as MIN_CLOSURE_THRESHOLD in the notebook logic and filter by support group closure count before saving the final CSV. At the end of the notebook, print the incident states that are not closed to summarize open ticket activity.
