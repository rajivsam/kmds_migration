[project]
name = "olist_migration"
version = "0.1.0"
description = "Migration of the Olist Example to a KMDS project"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "kmds-data-helper>=0.5.0",
    "kmds-ui>=0.1.0",
    "pyarrow>=12.0.0",
    "dd-parser-cleaner>=1.5.0",
    "kmds-featurization==0.8.1",
    "kmds-modeling==0.6.0",
]

[dependency-groups]
dev = ["ipykernel>=6.29.0"]

[tool.uv]
package = false
